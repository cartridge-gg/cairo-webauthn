- add a method to the interface to store the public key
- add a method to retrieve the public key
- test the set/get of the public key
- in the webauthn_verify verify against the stored public key instead of the passed one
- test that it works
- dont pass the challenge arg
    - retrieve the challenge in cairo
    - if problems arise enquire what encoding is used and make it so that it works
- test manually invoking the webauthn_verify
- modify the __validate__ method so that it dynamically decides on the auth method
    - add an enum to rust for various auth methods
    - add coresponding enum/consts to cairo
- test the __validate__ and __execute__ methods with new options
- change Option to HashMap when storing the public key(s)
- test multiple signers 